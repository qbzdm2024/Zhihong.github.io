sentence-transformers>=2.7
scikit-learn>=1.4
thefuzz>=0.22
python-Levenshtein>=0.25
openai>=1.30
transformers>=4.41
accelerate>=0.30
# bitsandbytes needs CUDA; on SageMaker GPU instances install with:
#   pip install bitsandbytes>=0.43
# It is optional — the pipeline falls back to float16 if not present.
bitsandbytes>=0.43
openpyxl>=3.1
tqdm>=4.66
boto3>=1.34
pandas>=2.2
numpy>=1.26
torch>=2.2
